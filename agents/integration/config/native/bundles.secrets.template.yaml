bundles:
  version: "1"
  items:
    - id: workspace@2026-03-31-13-36
      # This demonstration adds no bundle-owned secret. Model and web-provider
      # credentials remain platform service secrets referenced by assembly.yaml.
      secrets: {}
