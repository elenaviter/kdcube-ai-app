# Everything the web_search MCP server needs beyond the Python 3.11+
# standard library and this repo on PYTHONPATH. This is the verified set
# (a clean venv with exactly these, plus the repo path, runs the server
# and the live tools end to end); newer versions usually work.
mcp==2.0.0
aiohttp==3.14.3
pydantic==2.13.5
pydantic-settings==2.15.0
PyYAML==6.0.3
requests==2.34.2
beautifulsoup4==4.15.0
pillow==12.3.0
redis==8.1.0
langchain-core==1.6.1
langchain-openai==1.6.0
connection-hub==2026.9.1.1120
# DuckDuckGo fallback backend (used when the primary search provider fails)
ddgs==9.16.0
