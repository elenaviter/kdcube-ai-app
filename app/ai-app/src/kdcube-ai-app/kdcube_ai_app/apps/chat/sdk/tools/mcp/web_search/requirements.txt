# Everything the web_search MCP server needs beyond the Python 3.11+
# standard library and this repo on PYTHONPATH. Versions mirror the
# platform's chat-processor requirements (requirements-chat.txt /
# requirements-chat-processor.txt at the repo source root) - the
# environment this pipeline runs in inside KDCube; deps the platform
# leaves unpinned stay unpinned here.
mcp==2.0.0
aiohttp==3.12.15
pydantic==2.13.4
pydantic-settings
PyYAML==6.0.2
requests==2.32.5
beautifulsoup4
Pillow
redis==6.2.0
langchain-core==1.2.6
langchain-openai==1.1.6
connection-hub>=2026.09.01.0000
certifi
# provider client for the neural roles (see TOOLS.md, "The neural
# pipeline"). Keep this exact pin: anthropic 1.x changed the streaming
# signature and silently breaks the segmenter.
anthropic==0.83.0
# DuckDuckGo fallback backend (used when the primary search provider fails)
ddgs
