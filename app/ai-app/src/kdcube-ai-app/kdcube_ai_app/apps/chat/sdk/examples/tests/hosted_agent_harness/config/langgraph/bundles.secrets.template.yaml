bundles:
  version: "1"
  items:
    - id: ported-langgraph-agents@2026-07-13
      # This demonstration adds no bundle-owned secret. KDCubeChatModel resolves
      # the configured platform model service inside the trusted runtime.
      secrets: {}
